import { computed, ref, reactive, onMounted, defineExpose, withCtx} from 'vue';
import './TopBarSeperator.css'

const topbarseperator = {
    components: {  },
    setup(props, ctx) {

        return { }
    },
    template: `
    <div class="topbarseperator"></div>
    `
} 

export { topbarseperator }